move to terraform dir for which you want to apply
terraform init
terraform plan
terraform apply
terraform destory

# for destroying and recreating the resouces
terraform taint <resource_type>.<resource_name>
terraform untaint <resource_type>.<resource_name>

terraform validate
terraform fmt
terraform workspace new <workspace_name>
terraform workspace select <workspace_name>